apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mswa-staging
  namespace: argocd
spec:
  destination:
    namespace: mswa-staging
    server: https://kubernetes.default.svc
  source:
    path: app
    repoURL: https://github.com/Mobility-Scooter-Project/mobility-scooter-infa
    targetRevision: bryan/tri-env-setup
    helm:
      valueFiles:
        - values-staging.yaml
  project: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: false
      selfHeal: true
