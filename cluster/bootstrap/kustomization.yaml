apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd
resources:
  - argocd-namespace.yaml
  # TODO: pin the version of ArgoCD
  - https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

patches:
  - path: patch.yaml
    target:
      kind: ConfigMap
      name: argocd-cmd-params-cm
  