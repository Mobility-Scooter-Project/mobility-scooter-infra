apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - role.yaml
  - serviceaccount.yaml
  - clusterrolebinding.yaml

commonLabels:
  app.kubernetes.io/name: rbac
  app.kubernetes.io/component: admin-access
  app.kubernetes.io/managed-by: kustomize
