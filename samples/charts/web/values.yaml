image:
  repository: ghcr.io/mobility-scooter-project/mobility-scooter-web-app/web
  tag: develop

replicas: 1

service:
  port: 3000
  targetPort: 3000

ingress:
  enabled: true
  host: localhost:3000
